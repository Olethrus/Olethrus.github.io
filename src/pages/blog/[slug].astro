---
import Layout from '../../components/Layout.astro';
const { slug } = Astro.params;
const content = {
  welcome: {
    title: 'Welcome',
    body: 'Shipping a lean, fast site on GitHub Pages with a clear path to business scale.'
  }
}[slug] ?? { title: 'Post', body: 'Coming soon.' };
---
<Layout title={`${content.title} — Blog`}>
  <h1>{content.title}</h1>
  <p>{content.body}</p>
</Layout>
