---
import Layout from '../components/Layout.astro';

const posts = [
  {
    slug: 'welcome',
    title: 'Welcome',
    description: 'Why this site exists and what you can expect.',
    date: '2025-09-27'
  },
];
---
<Layout title="William Controls | Home" description="Industrial automation & AI built for scale.">
  <h1>Welcome to William Controls</h1>
  <  <h2>Latest Updates</h2>
  <ul>
    {posts.map(post => (
      <li>
        <a href={`/blog/${post.slug}`}>{post.title}</a> — <small>{post.date}</small><br />
        {post.description}
      </li>
    ))}
  </ul>
</Layout>
