---
import Layout from '../components/Layout.astro';

const posts = [
  {
    slug: 'welcome',
    title: 'Welcome',
    description: 'Why this site exists and what you can expect.',
    date: '2025-09-27'
  },
];
---
<Layout title="William Controls | Home" description="Industrial automation & AI built for scale.">
  <h1>Welcome to William Controls</h1>
  <p>I am a controls engineer lead with expertise in PLC/HMI, SCADA, UNS architectures, and AI integration for manufacturing. This site shares insights on technologies and vendors shaping the future of industrial automation.</p>
  <h2>Latest Updates</h2>
  <ul>
    {posts.map(post => (
      <li>
        <a href={`/blog/${post.slug}`}>{post.title}</a> — <small>{post.date}</small><br />
        {post.description}
      </li>
    ))}
  </ul>
</Layout>
